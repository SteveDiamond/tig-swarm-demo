[project]
name = "dflash-swarm"
version = "0.1.0"
description = "DFlash training reverse-engineering swarm"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1",
    "transformers>=4.57",
    "datasets",
    "huggingface-hub",
    "accelerate",
    "numpy",
    "tqdm",
    "requests",
]
